function Shape(x, y) {
    var lastModified;
    var connStr;

    function runTask() {
    }

    return {
        dump: function () {
            console.log(x + ", " + y);
        },
        //move: function (dx, dy) {
        //    x += dx;
        //    y += dy;
        //}
    };

}
